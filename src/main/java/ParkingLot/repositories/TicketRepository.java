package ParkingLot.repositories;

import ParkingLot.models.Ticket;

public class TicketRepository {
    public Ticket save(Ticket ticket) {
        // Logic to save the ticket to the database
        return ticket;
    }
}
